import React, { Fragment } from 'react';
import subBanner from '@/assets/banner/subBanner.jpg'

const SubBanner = () => (
  <div>
    <img src={subBanner} style={{width: '100%', height: '600px'}}/>
  </div>
);
export default SubBanner;
